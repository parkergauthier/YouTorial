CREATE TABLE analytics(
    videoID varchar,
    views_count numeric,
    likes numeric,
    comments_ numeric,
    length_ interval,  
    like_ratios numeric,
    comment_ratio numeric,
    polarity numeric,
    subjectivity numeric
);